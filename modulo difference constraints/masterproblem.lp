% introduce a flow constrain for each transport link within the system 

{} :- transportLink(From,To, _).


% remove those flow constrains which exceed the capacity allowed on that transport link. This capacity is defined with the route 



